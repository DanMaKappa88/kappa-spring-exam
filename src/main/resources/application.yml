
server:
  port: 8081

logging:
  level:
    hu.flowacademy.bands: TRACE

spring:
  datasource:
    url: jdbc:h2:file:./db/demo
    driverClassName: org.h2.Driver
    username: root
    password: P4ssword+
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create

  h2:
    console:
      enabled: true